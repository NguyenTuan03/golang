package config

const (
	// PostgreSQL connection string template
	PostgresConnectionStringTemplate = "host=%s port=%s user=%s password=%s dbname=%s sslmode=%s"

	// Database driver names
	DriverPostgres = "postgres"
)
